@using DomainModel.Entities;
@model IEnumerable<Song>

@{
    ViewBag.Title = "Index";
}

@Html.Action("Navigation", "Nav")

@foreach (var song in Model)
{    
    Html.RenderPartial("SongSummary", song);
}

